version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: planhigh_crm_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: planhigh_crm
    ports:
      - "5432:5432"
